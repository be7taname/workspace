package problemset;

public class AddBinary {
	public String solution(String a, String b)
	{
		int numA, numB;
		numA = numB = 0;
		for (int i = 0; i < a.length(); i++) {
			if (a.charAt(i) == '1')
		}
	}
}
