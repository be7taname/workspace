package problemset;

public class IsAnagram {
	public boolean solution(String s, String t) {
		if (s.length() != s.length()) return false;
		
		while (s.length() != 0) {
			s.replaceAll(String.valueOf(s.charAt(0)), "");
		}
	}
}
