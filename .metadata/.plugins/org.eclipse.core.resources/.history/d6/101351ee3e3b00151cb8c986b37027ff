package problemset;

public class MyAtoi {
	public int solution(String str)
	{
		String trimStr = str.trim();
		
		int d = Character.getNumericValue(trimStr.charAt(0));
		if (d == -1) return 0;
		for (int i = 0; i < trimStr.length(); i++) {
			
		}
	}
}
